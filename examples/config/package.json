{
  "name": "@augejs/examples-config",
  "scripts": {
    "start": "ts-node ./src/main.ts",
    "prebuild": "rimraf dist",
    "build": "tsc --build ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@augejs/core": "^1.0.6",
    "@augejs/file-config": "^1.2.5"
  }
}
